"use client";

// UserCard – displays a single user's info (name, email, company)
export default function UserCard({ user }) {
  return (
    <div className="glass p-4">
      <p className="text-muted">UserCard – to be built</p>
    </div>
  );
}
